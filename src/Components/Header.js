import React from "react";

const Header = () => {
    const title = `Weather App in React`;
    return (
        <>
            <div className="heading">
                {`${title}`}
            </div>
        </>

    )
}
export default Header;
